function rentalCarCost(d) {
  var DayDiscount = 0, TotalAmount = 0, WeekDiscount = 0;
  if (d >= 3 && d < 7) {
      DayDiscount = 20;
  }
  else if (d >= 7) {
      WeekDiscount = 50;
  }
  TotalAmount = (d * 40) - DayDiscount - WeekDiscount;
  return TotalAmount;
}
